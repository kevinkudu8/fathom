# Copy this file to .env.local and paste your real key.
#   cp .env.local.example .env.local
#
# Server-side only — used exclusively in app/api/* route handlers.
# NEVER prefix this with NEXT_PUBLIC_ and never import it into a client component.
ANTHROPIC_API_KEY=
